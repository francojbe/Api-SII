# --- CONFIGURACIÓN BACKEND (Python / FastAPI) ---

# Clave para proteger TU propia API de scraping (el cliente debe enviarla en x-api-key header)
API_KEY_SCII=mi_llave_secreta_123

# URL de tu Proxy de IA (Multi-IA Service)
AI_API_URL=https://recuperadora-api-ia-free.nojauc.easypanel.host/v1/chat/completions

# Clave de autenticación para tu Proxy de IA
AI_API_KEY=mi_proxy_secreto

# Puerto del servidor (Interno para Docker)
PORT=8001


# --- CONFIGURACIÓN FRONTEND (Vite / React) ---
# Nota: En producción, esto se suele configurar al momento de hacer el "build" del frontend.

# URL pública de tu backend de scraping (donde está alojado este microservicio)
VITE_API_URL=https://tu-dominio-backend.easypanel.host
